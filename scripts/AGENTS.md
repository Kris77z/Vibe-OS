# Workspace AGENTS Compatibility Note

`/Users/jungle/Desktop/dev/vibe-os/AGENTS.md` is now the canonical workspace prompt file for OpenClaw.

This file is kept only as a compatibility marker for older notes and docs that previously referenced `scripts/AGENTS.md`.

If you need to update the agent behavior contract, edit the root `AGENTS.md` instead.
